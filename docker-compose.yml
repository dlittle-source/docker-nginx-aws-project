services:
  web:
    build: .
    image: project2-nginx-app
    container_name: project2-container

    ports:
      - "8080:80"

    restart: always

    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3
